# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
/lib/
/lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
venv310/
.venv/
ENV/
env/

# Node / Frontend
node_modules/
frontend/dist/
frontend/dist-ssr/
*.local

# Built SPA copied into the backend for single-URL deploys. The Dockerfile
# produces this at build time, so it should never be committed.
backend/static/app/

# Environment Variables
.env
.env.local
.env.*.local

# Secrets (YouTube OAuth client secret + token) - NEVER commit
backend/secrets/
*_client_secret.json
*_token.json
# Scratch copy of the token for pasting into a host's env vars
token_oneline.txt

# Logs
*.log
app_debug.log
app.log

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Project Specific
backend/uploads/*
!backend/uploads/.gitkeep
backend/resources/images/*
!backend/resources/images/.gitkeep
backend/resources/audio/*
!backend/resources/audio/.gitkeep
backend/resources/video/*
!backend/resources/video/.gitkeep
backend/resources/scripts/script.json
# static/videos is NOT ignored: commit the videos (+ .jpg thumbnails) you want to
# appear on the deployed site. They ship with the deploy and the Library lists them.
backend/static/videos/*.log

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# Docker
*.pid
*.seed
*.pid.lock

# Temporary files
*.tmp
*.temp
.cache/
