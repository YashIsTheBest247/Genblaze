# FastAPI and ASGI Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0
python-multipart==0.0.6

# Core Dependencies
python-dotenv
google-genai
beautifulsoup4
requests

# Genblaze - generative media orchestration + provenance manifests,
# and Backblaze B2 (S3-compatible) durable storage for every artefact.
genblaze-core
genblaze-s3
genblaze-google
genblaze-gmicloud

# Trending pipeline (RSS monitoring + scheduling)
feedparser
APScheduler

# YouTube Data API (auto-publishing)
google-api-python-client
google-auth
google-auth-oauthlib
google-auth-httplib2

# Video Processing
moviepy
imageio-ffmpeg
pysrt
pillow

# Audio/TTS — Edge TTS is the default narration backend: free, no API key, no
# local model, negligible memory. Kokoro (and therefore torch, ~490 MB of deps
# and a ~1.5 GB peak) is OPTIONAL and lives in requirements-kokoro.txt.
# Keeping torch out of the default image is what makes this deployable on a
# small instance — with it in, the container was OOM-killed on every render.
edge-tts
