# FastAPI and ASGI Server
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0
python-multipart==0.0.6

# Core Dependencies
python-dotenv
google-genai
beautifulsoup4
requests

# Trending pipeline (RSS monitoring + scheduling)
feedparser
APScheduler

# YouTube Data API (auto-publishing)
google-api-python-client
google-auth
google-auth-oauthlib
google-auth-httplib2

# Video Processing
opencv-python-headless
moviepy
imageio-ffmpeg
pysrt
pillow

# Audio/TTS
soundfile
kokoro
misaki

# ML/AI
# CPU-only torch: the +cpu local version lives on the pytorch index and keeps the
# build lean (default PyPI torch on Linux pulls the multi-GB CUDA wheel).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.1+cpu
transformers
